# 연산 분담과 실시간성 설계

### 연산 분담 배치표

| 작업 | 위치 | 설계 지연 예산 | 데이터량 | 근거 |
|---|---|---|---|---|
| 모터 속도 제어 | 임베디드 | 제어 job마다 1 ms | 엔코더 채널 수 n, 채널당 B_e byte이면 2000 n B_e B/s | 피드백과 PWM을 로컬에서 처리해야 네트워크 단절 시에도 제어 가능 |
| 장애물 감지 | Edge AI | 스캔 수신 후 30 ms | 스캔당 N개 float32 거리이면 60 N B/s, 헤더 제외 | 66.67 ms 입력 주기 안에 검출; 경량 정지 판단과 watchdog을 로컬에서 수행 |
| 보행자 인식 | Edge AI | 선택한 영상 수신 후 50 ms | RGB8 입력 165.888 MB/s; 검출 결과 크기는 모델에 따라 달라짐 | 전송량이 LTE 용량보다 크고 통신 단절 시에도 인식 필요; 인식 20 Hz 설계 |
| 지도 기반 경로 계획 | Edge AI | 갱신 요청 후 1 s | 지도 해상도·크기가 미지정이므로 정량 확정 불가 | 로컬 지도로 통신 없이 계획; 대용량 지도 배포·장기 통계는 클라우드 보조 가능 |
| 배달 완료 사진 업로드 | 클라우드 | 이벤트 후 2 s 목표, 실패 시 로컬 큐에 보관 | 원시 1장 2.7648 MB; 압축 파일 크기는 실측 필요 | 촬영·압축은 Edge, 서버 저장은 클라우드; 지연돼도 제어 마감에 영향 없음 |
| 운행 로그 집계 | 클라우드 | 10 s 배치 집계 | 레코드 크기 B_l, 생성율 f_l이면 B_l f_l B/s | 로봇에서 기록·버퍼링하고 클라우드에서 장기 집계; 단절 시 재전송 |

### 원시 카메라 전송량

```text
1 frame = 1280 × 720 × 3 = 2,764,800 B = 2.7648 MB
60 fps  = 2,764,800 × 60 = 165,888,000 B/s = 165.888 MB/s
bit/s   = 165,888,000 × 8 = 1,327,104,000 bit/s = 1327.104 Mbps
```

| 업링크 조건 | 최대 payload 상한 | 필요량/용량 | 1프레임 직렬화 시간 |
|---|---:|---:|---:|
| 95 Mbps | 11.875 MB/s | 13.9695배 | 232.878 ms |
| 100 Mbps | 12.5 MB/s | 13.2710배 | 221.184 ms |

16.67 ms마다 프레임이 오는데 한 프레임 전송에 221~233 ms가 필요하다.
100 Mbps에서도 큐는 최소 153.388 MB/s씩 증가한다. 프로토콜 오버헤드를
제외한 낙관적인 계산에서도 성립하지 않는다. 영상 추론은 Edge에서 수행하고
필요한 검출 결과나 압축 이미지·이벤트만 전송한다.

### 계층과 멀티레이트 흐름

| 계층/기능 | 입력 | 설계 갱신율·주기 | 다음 단계 |
|---|---|---|---|
| 인지: 장애물 감지 | 라이다 15 Hz | 15 Hz, 66.67 ms | 로컬 회피/정지 판단 |
| 인지: 보행자 인식 | 영상 60 fps | 20 Hz, 50 ms | 보행자 감속 판단 |
| 인지: 자세 추정 보조 | IMU 400 Hz·엔코더 2 kHz | 융합 100 Hz, 10 ms 제안 | 경로/목표속도 판단 |
| 판단: 지도 경로 계획 | 지도·목적지·현재 자세 | 1 Hz, 1 s | 로컬 경로 추종 |
| 판단: 로컬 안전·목표속도 | 인지 결과와 timestamp | 50 Hz, 20 ms; 긴급 정지 이벤트 즉시 처리 | 임베디드 목표 속도 |
| 제어: 모터 속도 제어 | 엔코더 2 kHz | 1 kHz, 1 ms | 모터 드라이버 |
| 운영 I/O: 사진 업로드 | 촬영 이벤트 | 이벤트 기반 | 클라우드 저장 |
| 운영 분석: 로그 집계 | 각 계층의 로그 | 10 s 배치 | 클라우드 집계 |

사진 업로드와 로그 집계 자체는 인지→판단→제어 폐루프의 기능이 아니다.
사진 **획득**은 인지에 연결되고 로그 **생성**은 모든 계층에 연결된다.
이를 억지로 제어 계층에 넣지 않고 운영 경로로 구분했다.


### 실시간성 분류

| 작업 | 등급 | 기준 및 마감 초과 결과 |
|---|---|---|
| 모터 속도 제어 | Hard | 마감 초과 시 제동/조향 출력이 늦어져 정지거리 증가 또는 주행 불안정이 발생할 수 있음 |
| 장애물 감지(안전 정지 경로) | Hard | 정지 판단을 시작할 마감을 넘기면 사람·장애물과 충돌할 수 있음 |
| 보행자 인식 | Firm | timestamp가 만료된 의미 인식 결과는 버림; 기하 기반 장애물 정지 경로를 별도로 유지한다는 설계 조건 |
| 지도 기반 경로 계획 | Firm | 1 s 유효기간을 넘긴 계획 결과는 폐기하고 현재 안전 경로/정지 동작 유지 |
| 사진 업로드 | Soft | 늦은 업로드도 배송 기록으로 유효; 재시도 가능 |
| 로그 집계 | Soft | 늦은 집계도 분석 가치가 남음 |

Hard는 요구사항의 분류이며 현재 일반 Ubuntu가 그 마감을 보장한다는 뜻이 아니다.
안전용 정지 제어는 독립 임베디드 계층과 watchdog, 검증된 실행시간 예산이 필요하다.

- **주기:** 모터 제어 작업을 반복하는 목표 시간 간격은 1 ms이다.
- **지연:** 라이다 스캔 수신부터 해당 장애물에 대한 정지 명령이 출력될 때까지 걸린 시간이다.
- **지터:** 모터 제어 실행 간격이 목표 1 ms에서 0.9/1.1 ms처럼 변동하는 정도이다.

## 지연 예산의 시작 시점과 네트워크 비교

위 지연은 센서 데이터 수신 후 처리 마감이다. 장애물 발생부터의 지연에는
15 Hz 라이다의 최대 66.67 ms 샘플 대기가 추가되므로 검출까지 최대
약 96.67 ms, 다음 1 ms 제어 주기까지 약 97.67 ms를 별도 예산으로 둔다.
이는 설계 목표이며 일반 Ubuntu에서 WCET를 실측/보장했다는 뜻이 아니다.

영상 요구량 1327.104 Mbps는 95/100 Mbps의 13.97/13.27배이다.
핑 15 ms는 RTT 측정값이지 단방향 7.5 ms나 최악 지연 보장이 아니다.
